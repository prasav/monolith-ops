// Placeholder for future interactivity (form handling, filters).
// The mailto: + profile links in index.html are the contact path for now.
document.getElementById('email-btn')?.addEventListener('click', () => {
  // Replace mailto in index.html with your real address before launch.
});
